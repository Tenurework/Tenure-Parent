{
  "name": "@tenure/identity",
  "version": "0.1.0",
  "private": true,
  "description": "The canonical identity model: a durable person, the external identities that authenticate them, effective-dated tenant memberships, invitations, sessions, authentication events and recovery paths — liveness computed from the clock, and no status change without an audit record.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
