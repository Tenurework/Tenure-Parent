# CAT-010 — connection counts, on the Bible's own examples

Generated by `tools/cat-connection-counts.mjs` from `packages/provisioning/src/connection-cardinality.mjs`. Do not edit by hand: `tests/architecture/cat-connection-counting.test.mjs` regenerates it and fails if the committed bytes differ, so an edit here is reverted by CI rather than believed.

Every number below is computed. Nothing is transcribed, and no scenario names a real customer, provider account or credential — they are specification shapes, prefixed `example-`, for the eight subjects CAT-010-005 names.

The engine implements 14 cardinality modes, 16 count dimensions, 5 count kinds and 9 of §4.2's detections; 5 more are deferred to CAT-030-003 and named at the end of this document.

## multi-Microsoft tenants — `example-multi-microsoft-tenant`

Two Entra tenants are two connection instances. The 52 mailboxes under them are selected resources, and §1 forbids counting one of them as one Microsoft tenant.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 2 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 52 |
| Entitled capacity — contractual maximum or billable units | not known — 2 of 2 connection instance(s) declare no entitled capacity: conn-outlook-a, conn-outlook-b. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 2 of 2 connection instance(s) declare no provisioned capacity: conn-outlook-a, conn-outlook-b. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 2 of 2 connection instance(s) declare no active usage: conn-outlook-a, conn-outlook-b. A portfolio total would report unmeasured capacity as zero. |

- `req-collaboration-email` (collaboration.email, ONE_PER_DIMENSION_VALUE): satisfied — every one of the 2 declared external_organization values has a connection
  - fragmentation: assessed

No §4.2 finding. Every condition this engine decides was checked and none fired.

## multi-Google tenants — `example-multi-google-tenant`

Two Workspace organizations satisfy AT_LEAST_N 2 on the arithmetic, and one of them is a personal grant — which §4.2 refuses for an organization-wide requirement because it ends when that person's account does.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 2 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 0 |
| Entitled capacity — contractual maximum or billable units | not known — 2 of 2 connection instance(s) declare no entitled capacity: conn-google-personal, conn-google-primary. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 2 of 2 connection instance(s) declare no provisioned capacity: conn-google-personal, conn-google-primary. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 2 of 2 connection instance(s) declare no active usage: conn-google-personal, conn-google-primary. A portfolio total would report unmeasured capacity as zero. |

- `req-collaboration-calendar` (collaboration.calendar, AT_LEAST_N): satisfied — at least 2 required, 2 configured
  - fragmentation: not assessed — no `cardinality.countBy` is declared

1 finding(s):

- **personal_grant_for_organization_requirement** (§4.2 "one personal connection satisfying an organization-wide requirement", CAT-010-004) — connection conn-google-personal is a personal grant, and collaboration.calendar is an organization-wide requirement. It ends when that person's account does.

## Slack workspaces — `example-slack-workspaces`

Three workspace connections, twenty-three channels. §1 forbids counting one Slack channel as one workspace connection, so the channels appear only under selected resources.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 23 |
| Entitled capacity — contractual maximum or billable units | not known — 3 of 3 connection instance(s) declare no entitled capacity: conn-slack-corp, conn-slack-field, conn-slack-partner. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 3 of 3 connection instance(s) declare no provisioned capacity: conn-slack-corp, conn-slack-field, conn-slack-partner. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 3 of 3 connection instance(s) declare no active usage: conn-slack-corp, conn-slack-field, conn-slack-partner. A portfolio total would report unmeasured capacity as zero. |

- `req-collaboration-chat` (collaboration.chat, ONE_PER_DIMENSION_VALUE): satisfied — every one of the 3 declared provider_workspace values has a connection
  - fragmentation: assessed

No §4.2 finding. Every condition this engine decides was checked and none fired.

## Salesforce environments — `example-salesforce-environments`

One connection assigned to production AND a UAT sandbox is the §4.2 reuse bullet, and the developer sandbox has no connection at all. Two different findings from one portfolio.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 1 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 3 |
| Entitled capacity — contractual maximum or billable units | not known — 1 of 1 connection instance(s) declare no entitled capacity: conn-salesforce-shared. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 1 of 1 connection instance(s) declare no provisioned capacity: conn-salesforce-shared. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 1 of 1 connection instance(s) declare no active usage: conn-salesforce-shared. A portfolio total would report unmeasured capacity as zero. |

- `req-crm-account` (crm.account, ONE_PER_DIMENSION_VALUE): NOT satisfied — 1 environment value(s) have no connection: sandbox-dev
  - fragmentation: assessed

2 finding(s):

- **missing_dimension_coverage** [dimension] (§4.2 "missing dimension coverage", CAT-010-004) — 1 declared environment value(s) have no connection: sandbox-dev.
- **unsafe_reuse_across_boundary** (§4.2 "one connection assigned across incompatible regions/legal entities", CAT-010-004) — connection conn-salesforce-shared is assigned across 2 environments (production, sandbox-uat) while crm.account requires separation by environments.

## ERP entities — `example-erp-legal-entities`

Four legal entities are in scope and three have a general-ledger connection. The fourth is missing dimension coverage, which is a blocker rather than a rounding difference.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 9 |
| Entitled capacity — contractual maximum or billable units | not known — 3 of 3 connection instance(s) declare no entitled capacity: conn-sap-entity-de, conn-sap-entity-sg, conn-sap-entity-us. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 3 of 3 connection instance(s) declare no provisioned capacity: conn-sap-entity-de, conn-sap-entity-sg, conn-sap-entity-us. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 3 of 3 connection instance(s) declare no active usage: conn-sap-entity-de, conn-sap-entity-sg, conn-sap-entity-us. A portfolio total would report unmeasured capacity as zero. |

- `req-finance-general-ledger` (finance.generalLedger, ONE_PER_DIMENSION_VALUE): NOT satisfied — 1 legal_entity value(s) have no connection: entity-br
  - fragmentation: assessed

1 finding(s):

- **missing_dimension_coverage** [dimension] (§4.2 "missing dimension coverage", CAT-010-004) — 1 declared legal_entity value(s) have no connection: entity-br.

## banks — `example-bank-channels`

A backup payment channel on the SAME bank identity as the primary is not a second path. §4.2 calls it unsafe concentration, and the mode's own verdict refuses it too.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 2 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 0 |
| Entitled capacity — contractual maximum or billable units | not known — 2 of 2 connection instance(s) declare no entitled capacity: conn-bank-backup, conn-bank-primary. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 2 of 2 connection instance(s) declare no provisioned capacity: conn-bank-backup, conn-bank-primary. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 2 of 2 connection instance(s) declare no active usage: conn-bank-backup, conn-bank-primary. A portfolio total would report unmeasured capacity as zero. |

- `req-treasury-payment-initiation` (treasury.paymentInitiation, PRIMARY_PLUS_BACKUP): NOT satisfied — the backup shares the primary's provider identity, so it is not a second path
  - fragmentation: not assessed — no `cardinality.countBy` is declared

2 finding(s):

- **duplicate_provider_identity** (§4.2 "duplicate provider account/workspace identity", CAT-010-004) — provider identity bank-account-0001 is connected 2 times for treasury.paymentInitiation: conn-bank-backup, conn-bank-primary.
- **unsafe_concentration** (§4.2 "unsafe concentration when primary-plus-backup is required", CAT-010-003) — backup conn-bank-backup uses provider identity bank-account-0001, which is also a primary. The same account failing takes both paths with it.

## Stripe accounts — `example-stripe-connected-accounts`

Three connected accounts for three merchant entities, all five counts declared. §1 forbids counting one connected account as one tenant, so the tenant count is nowhere in this ledger.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 6 |
| Entitled capacity — contractual maximum or billable units | 8 |
| Provisioned capacity — AWS/runtime resources reserved | 4 |
| Active usage — current resources/users/objects/events/actions | 2 |

- `req-payments-charge` (payments.charge, ONE_PER_DIMENSION_VALUE): satisfied — every one of the 3 declared merchant_entity values has a connection
  - fragmentation: assessed

No §4.2 finding. Every condition this engine decides was checked and none fired.

## plants — `example-plants`

Two separate MES identities serve the same plant. Coverage is complete and the count still costs twice — two token renewals, two quotas, two workers for one unit of coverage.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 0 |
| Entitled capacity — contractual maximum or billable units | not known — 3 of 3 connection instance(s) declare no entitled capacity: conn-mes-hamburg-a, conn-mes-hamburg-b, conn-mes-monterrey. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 3 of 3 connection instance(s) declare no provisioned capacity: conn-mes-hamburg-a, conn-mes-hamburg-b, conn-mes-monterrey. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 3 of 3 connection instance(s) declare no active usage: conn-mes-hamburg-a, conn-mes-hamburg-b, conn-mes-monterrey. A portfolio total would report unmeasured capacity as zero. |

- `req-manufacturing-work-order` (manufacturing.workOrder, ONE_PER_DIMENSION_VALUE): satisfied — every one of the 2 declared warehouse values has a connection
  - fragmentation: assessed

1 finding(s):

- **excessive_fragmentation** (§4.2 "excessive fragmentation that causes avoidable cost or operational load", CAT-010-004) — 2 separate siemens.opcenter identities serve the same warehouse value "plant-hamburg" (mes-site-hamburg-a, mes-site-hamburg-b), and none is a declared backup.

## partners — `example-partners`

Four trading partners were discovered and two approved. DISCOVERED_THEN_APPROVED counts the approved ones only, so a discovery sweep cannot satisfy a requirement by itself.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 4 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 0 |
| Entitled capacity — contractual maximum or billable units | not known — 4 of 4 connection instance(s) declare no entitled capacity: conn-edi-partner-alpha, conn-edi-partner-bravo, conn-edi-partner-charlie, conn-edi-partner-delta. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 4 of 4 connection instance(s) declare no provisioned capacity: conn-edi-partner-alpha, conn-edi-partner-bravo, conn-edi-partner-charlie, conn-edi-partner-delta. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 4 of 4 connection instance(s) declare no active usage: conn-edi-partner-alpha, conn-edi-partner-bravo, conn-edi-partner-charlie, conn-edi-partner-delta. A portfolio total would report unmeasured capacity as zero. |

- `req-supply-chain-purchase-order` (supplyChain.purchaseOrder, DISCOVERED_THEN_APPROVED): NOT satisfied — 2 approved of 3 required; 2 discovered instance(s) do not count until approved
  - fragmentation: not assessed — no `cardinality.countBy` is declared

1 finding(s):

- **below_minimum** [capability] (§4.2 "fewer instances than the capability minimum", CAT-010-003) — supplyChain.purchaseOrder declares a minimum of 3 connection instance(s); 2 configured.

## ERP entities on incompatible ERPs — `example-unsupported-provider-mix`

Three legal entities, three different ERPs, and a capability that does not allow a mix. One of the three is not in the eligible set at all — §4.2 separates 'unsupported mix' from 'provider nobody certified', and both fire here.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 3 |
| Entitled capacity — contractual maximum or billable units | not known — 3 of 3 connection instance(s) declare no entitled capacity: conn-erp-entity-de, conn-erp-entity-sg, conn-erp-entity-us. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 3 of 3 connection instance(s) declare no provisioned capacity: conn-erp-entity-de, conn-erp-entity-sg, conn-erp-entity-us. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 3 of 3 connection instance(s) declare no active usage: conn-erp-entity-de, conn-erp-entity-sg, conn-erp-entity-us. A portfolio total would report unmeasured capacity as zero. |

- `req-finance-general-ledger-mixed` (finance.generalLedger, ONE_PER_DIMENSION_VALUE): satisfied — every one of the 3 declared legal_entity values has a connection
  - fragmentation: assessed

2 finding(s):

- **unsupported_provider_mix** (§4.2 "unsupported provider mix", CAT-010-004) — netsuite.erp is not in finance.generalLedger's eligible provider set (sap.s4hana, oracle.fusion-erp).
- **unsupported_provider_mix** (§4.2 "unsupported provider mix", CAT-010-004) — finance.generalLedger does not allow mixed providers; 3 are connected (netsuite.erp, oracle.fusion-erp, sap.s4hana).

## portfolio limits at every grain — `example-portfolio-limits`

CAT-010-003 asks for minimums and maximums per tenant, module, pack, capability, provider and dimension. One portfolio breaching all six, so each grain has a finding of its own.

| count kind | value |
| --- | --- |
| Connection instances — authenticated provider accounts/organizations | 3 |
| Selected resources — sites, channels, folders, mailboxes, calendars, etc. under a connection | 0 |
| Entitled capacity — contractual maximum or billable units | not known — 3 of 3 connection instance(s) declare no entitled capacity: conn-limits-1, conn-limits-2, conn-limits-3. A portfolio total would report unmeasured capacity as zero. |
| Provisioned capacity — AWS/runtime resources reserved | not known — 3 of 3 connection instance(s) declare no provisioned capacity: conn-limits-1, conn-limits-2, conn-limits-3. A portfolio total would report unmeasured capacity as zero. |
| Active usage — current resources/users/objects/events/actions | not known — 3 of 3 connection instance(s) declare no active usage: conn-limits-1, conn-limits-2, conn-limits-3. A portfolio total would report unmeasured capacity as zero. |

- `req-limits-email` (collaboration.email, ZERO_OR_MORE): satisfied — any number allowed, 3 configured
  - fragmentation: assessed

7 finding(s):

- **above_maximum** [module] (§4.2 "more instances than plan/tenant maximum", CAT-010-003) — module collaboration allows at most 1 connection instance(s); 3 are configured.
- **above_maximum** [capability] (§4.2 "more instances than plan/tenant maximum", CAT-010-003) — capability collaboration.email allows at most 1 connection instance(s); 3 are configured.
- **above_maximum** [dimension] (§4.2 "more instances than plan/tenant maximum", CAT-010-003) — external_organization "shared-org" allows at most 1 connection instance(s); 3 are configured.
- **above_maximum** [provider] (§4.2 "more instances than plan/tenant maximum", CAT-010-003) — provider microsoft.outlook-mail allows at most 1 connection instance(s); 3 are configured.
- **above_maximum** [tenant] (§4.2 "more instances than plan/tenant maximum", CAT-010-003) — the tenant may hold at most 2 connection instance(s); 3 are configured.
- **below_minimum** [pack] (§4.2 "fewer instances than the capability minimum", CAT-010-003) — pack workspace-pack requires at least 4 connection instance(s); 3 are configured.
- **excessive_fragmentation** (§4.2 "excessive fragmentation that causes avoidable cost or operational load", CAT-010-004) — 3 separate microsoft.outlook-mail identities serve the same external_organization value "shared-org" (entra-tenant-1, entra-tenant-2, entra-tenant-3), and none is a declared backup.

## §4.2 conditions this engine does not decide

Named rather than omitted. Each needs a fact the count engine is never given, and each is owned by the requirement in the last column.

| §4.2 bullet | needs | owner |
| --- | --- | --- |
| a provider instance with no capability consumer | the module/pack capability consumer graph | CAT-030-003 |
| a module/pack requiring a connector not selected | each module/pack's declared connector requirements | CAT-030-003 |
| conflicting field/system-of-record ownership | SystemOfRecordMapping and objectAuthority, which live on the tenant manifest | CAT-030-003 |
| no technical/business owner or owner departing before go-live | owner records with departure dates | CAT-030-003 |
| uncosted, unlicensed, unreviewed, or uncertified go-live dependency | the cost plan, licence state and provider review state | CAT-030-003 |
