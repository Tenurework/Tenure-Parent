<!-- Generated by tools/hcm-people-inventory.mjs. Do not edit by hand. -->

# People, member, seat, role and onboarding logic that exists today

HCM-000-001. Derived from the tree by `tools/hcm-people-inventory.mjs` and held
current by `tests/architecture/hcm-people-inventory-is-current.test.mjs`.

The People, HR and Workforce source document names 45 canonical objects and
10 distinctions it requires be kept apart. This repository runs a student
organisation roster. Both are true; the tables below are the distance between
them, measured rather than described.

**Of the 45 canonical objects: 1 PRESENT, 7 PARTIAL, 37 ABSENT.**

Every row names a path that exists, and every declared row names an anchor string
that must still appear inside that path. `PARTIAL` means something a reader could
mistake for the object exists — never that it works.

## 1. Tables that hold people, membership, seat or assignment data

`apps/web/prisma/schema.prisma` declares 52 models. 4 of them ARE the workforce core
(`DirectoryPerson`, `InstitutionMembership`, `Role`, `Seat`) and 6 own a relation into it
— 9 distinct tables, since `Seat` is both. Every one is classified below, and a
new table hung off any of the four reds the guard until somebody classifies it.

| Model | Concept it serves | What it actually is |
| --- | --- | --- |
| `DirectoryPerson` | Person | The roster of real people, seeded from the office spreadsheets. Name, email, kind, affiliation — no identity resolution, no privacy subject, no duplicate handling. |
| `InstitutionMembership` | Member | Membership of an institution with a role, a status and an effective window (`effectiveFrom`/`effectiveUntil`/`status`). The closest thing here to an employment relationship, and it is not one. |
| `LedgerEntry` | Seat attribution | Finance rows carry `postedBySeat` and `attributedSeat` so a posting outlives its author. People logic reaching into another domain, deliberately. |
| `MemoryRecord` | Seat memory | Institutional memory attached to a `Role`. This is the mechanism the source document's continuity chapter needs, wired to the conflated row rather than to `Seat`. |
| `OrganizationAdvisor` | Member (advisory) | A staff advisor attached to an organisation. A relationship, not an assignment: no dates, no status, no authority. |
| `Role` | Job + Seat, conflated | Carries the seat's title AND the authority it confers (`templateKey`). The source document keeps `Job` (reusable classification) and `Seat` (durable responsibility) apart; this row is both. |
| `RoleAssignment` | Assignment (date-grained) | The second, incompatible assignment shape: `startDate`/`endDate` and a SHADOW/ACTIVE/ALUMNI status. Two assignment tables with different time grains is the single largest defect in the people model here. |
| `Seat` | Position / Seat | The durable position, split out of `Role` so renaming a title cannot move authority. Effective-dated: `effectiveFrom`, `effectiveUntil`, `retiredAt`, plus `positionCode`. |
| `SeatHolding` | Assignment (term-grained) | Who held a seat in an academic year — `term` is a string like "2026-2027", not a date range. Past terms are kept rather than overwritten. |

3 more people tables hang off `User` rather than off the workforce core, so
they cannot be derived and are declared:

| Model | Concept it serves | What it actually is |
| --- | --- | --- |
| `ApprovalDelegation` | Delegation | Temporary scoped authority that is not a new assignment, exactly as the source document requires — for approval gates only. |
| `RoleTransfer` | TransitionPlan | A person-to-person handover of an institution role with PENDING/COMPLETED/DECLINED/CANCELLED and a `stepDownRole`. Institution-level only — it cannot transfer a club seat. |
| `User` | Person (authenticated) | The second person table. A `User` and a `DirectoryPerson` describing the same human are joined by email and nothing else. |

Derived relations into the core, for the reader who wants to check the
classification rather than trust it:

| Model | Relation into |
| --- | --- |
| `LedgerEntry` | `Seat` |
| `MemoryRecord` | `Role` |
| `OrganizationAdvisor` | `DirectoryPerson` |
| `RoleAssignment` | `Role` |
| `Seat` | `Role` |
| `SeatHolding` | `DirectoryPerson`, `Role` |

No model links into the workforce core and belongs to another domain. The first
one that does gets a row with a reason rather than being folded in silently.

## 2. Code that decides membership, authority, seats and transitions

23 modules, each verified to exist and to still contain its anchor.

| Path | Concept | Anchor | What it does |
| --- | --- | --- | --- |
| `apps/web/src/app/(app)/admin/people/page.tsx` | Transition | `RoleTransferPanel` | The institution-level people console where a role transfer is started. |
| `apps/web/src/app/(app)/orgs/[slug]/handoff/page.tsx` | Transition | `The term-transition handoff packet` | The term-transition packet: open work, finances, deadlines and contacts for a successor, assembled at read time. |
| `apps/web/src/app/(app)/orgs/[slug]/members/actions.ts` | Assignment | `transitionAssignment` | The server action that moves a person between SHADOW, ACTIVE and ALUMNI on a club seat. |
| `apps/web/src/components/admin/RoleTransferPanel.tsx` | Transition | `RoleTransferPanel` | The transfer UI: propose, accept, decline, cancel. |
| `apps/web/src/lib/clubs.ts` | Position | `uniquePositionCode` | Allocates the permanent position code a seat keeps across holders. |
| `apps/web/src/lib/delegation.ts` | Delegation | `effectiveApprovalContext` | Resolves who may act on an approval gate once delegations are applied. |
| `apps/web/src/lib/directory.ts` | Person | `DirectoryProvider` | The seam the roster is read through, so an LDAP/SCIM source can replace the seeded spreadsheet. |
| `apps/web/src/lib/identity/access-report.ts` | Member | `accessReportFor` | What one principal can currently reach, assembled for a privacy answer. |
| `apps/web/src/lib/identity/live-membership.ts` | Member | `membershipLiveness` | "Is a member right now" as a Prisma `where` fragment, checked against the package definition by its own test. |
| `apps/web/src/lib/org/projection.ts` | Org structure | `buildOrgGraph` | Projects `Institution` + `Organization` onto the configurable org graph. Imported by nothing but its own test. |
| `apps/web/src/lib/seat-is-not-a-role.itest.ts` | Seat | `the database agrees that a seat is not a role` | The integration test that fails if the position identity is merged back into the authority row. |
| `modules/index.ts` | Module manifest | `The organizations themselves, their rosters, and the seats people hold on them.` | The roster module's own 17-axis assessment, including the gaps it declares. |
| `packages/authorization/src/role-templates.ts` | Job/authority | `ROLE_TEMPLATES` | The named authority bundles a seat's `templateKey` points at, which is what replaced a regex over the seat title. |
| `packages/identity/src/effective-state.ts` | Member | `membershipLiveness` | The authority on whether a membership, identity or session is live at an instant. |
| `packages/identity/src/scim.ts` | Person (inbound) | `interpretScimPatch` | SCIM filter, patch and paging semantics — the shape an external HR system would push people through. No route serves it. |
| `packages/identity/src/seats.ts` | Assignment | `GE-040-003` | Simultaneous seats across tenants, and the overlap boundary a handover turns on. |
| `packages/identity/src/transitions.ts` | Member | `reviseMembership` | The only way to change a membership state, returning the audit record with it so the write cannot be made without one. |
| `packages/organization-model/src/assignment-states.ts` | Assignment | `PLATFORM_ASSIGNMENT_STATES` | The assignment state catalogue and the authority each state carries. |
| `packages/organization-model/src/bitemporal.ts` | Historical reconstruction | `resolveAsOf` | Valid-time and record-time resolution, corrections that preserve prior truth, and drift detection. |
| `packages/organization-model/src/continuity.ts` | Seat continuity | `succeedsTo` | What survives turnover: which resources follow the seat and which stay with the person. |
| `packages/organization-model/src/graph.ts` | Org structure | `asOf` | Structure is asked of a dated snapshot, never of the graph — so an approval routed in March is explicable against March's structure. |
| `packages/organization-model/src/position-lifecycle.ts` | Position | `planTermTransition` | Freeze, archive, split, merge, transfer and term-transition a position. |
| `packages/organization-model/src/succession-release.ts` | Succession | `releaseToSuccessor` | Classifies each resource a leaver holds and decides what the successor receives. |

## 3. The workforce modelling package, and what reaches it

`packages/organization-model` exports 89 symbols covering effective-dated
structure, assignment states, bitemporal correction, position lifecycle and
succession release. Importers are counted by module specifier across
`apps/web/src`, `apps/system-studio/src`, `packages`, `modules`, `blueprints`; a relative import from a sibling
file is not counted, which is stated here rather than papered over.

| Importer | Test only | Symbols taken |
| --- | --- | --- |
| `apps/system-studio/src/app/page.tsx` | no | `validateTopology` |
| `apps/system-studio/src/lib/command-handlers.ts` | no | `validateTopology` |
| `apps/web/src/lib/org/projection.test.ts` | yes | `validateTopology` |
| `apps/web/src/lib/org/projection.ts` | no | `OrgGraph`, `OrgTopology`, `OrgUnitInput`, `buildOrgGraph` |
| `apps/web/src/lib/people/seat-memory-boundary.ts` | no | `Classification`, `ClassifiedResource`, `HandoverSummary`, `InheritanceClass`, `ReleaseDecision`, `SuccessionContext`, `planHandover`, `releaseToSuccessor` |
| `apps/web/src/lib/workflows/generality-parity.test.ts` | yes | `OrgUnitInput`, `buildOrgGraph`, `validateTopology` |
| `blueprints/types.ts` | no | `OrgTopology` |
| `packages/generality-fixtures/src/corporate-org.test.ts` | yes | `OrgGraphError`, `buildOrgGraph`, `typeHoldsSeats` |
| `packages/generality-fixtures/src/corporate-org.ts` | no | `OrgGraph`, `OrgTopology`, `OrgUnitInput`, `buildOrgGraph`, `typeHoldsSeats` |
| `packages/generality-fixtures/src/corporate-purchase.test.ts` | yes | `Delegation` |
| `packages/generality-fixtures/src/corporate-purchase.ts` | no | `Delegation`, `DelegationVerdict`, `delegationAllows` |
| `packages/platform-config/src/build-system.ts` | no | `validateTopology` |

**71 of the 89 exported symbols are imported by nothing outside the package.**
17 are reached by non-test code.

Attributed to the source module each symbol is exported from — derived from
`index.ts`'s own `from "./module"` grouping, so this sentence cannot go stale
the way a hand-written one did:

- reached by non-test code: `continuity` (4/20), `graph` (3/8), `succession-release` (7/13), `topology` (3/9)
- reached by nothing outside the package: `assignment-states`, `bitemporal`, `position-lifecycle`

So the modelling that would answer "what did the organisation look like in
March" is still written, tested inside its own package, and connected to no
caller. That is the finding this section exists for.

Reached by nothing outside `packages/organization-model`:

- `AssignmentState`
- `AssignmentStateCatalog`
- `BitemporalVersion`
- `CatalogProblem`
- `ContainmentRule`
- `CorrectionOutcome`
- `CorrectionRefusal`
- `Dated`
- `DelegationRefusal`
- `EndOutcome`
- `EndRefusal`
- `EndableAssignment`
- `HistoryEntry`
- `LivePosition`
- `OperationContext`
- `OrgRelationInput`
- `OrgRelationType`
- `OrgSnapshot`
- `OrgUnitType`
- `PLATFORM_ASSIGNMENT_STATES`
- `Parentage`
- `PositionOperation`
- `PositionRefusal`
- `PositionRefused`
- `RecordPeriod`
- `ReleaseAction`
- `ReleasePolicy`
- `Resolution`
- `ResolutionRefusal`
- `ResolvedUnit`
- `ResourceOwnerKind`
- `ResourceRelationship`
- `Seat`
- `SeatOwnedResource`
- `SplitPart`
- `StateAuthority`
- `SuccessionOutcome`
- `Team`
- `TeamMembership`
- `TemporalQuery`
- `TermTransition`
- `TopologyError`
- `TransitionPlan`
- `ValidPeriod`
- `archivePosition`
- `assignmentProblems`
- `attachmentSurvivesTurnover`
- `correct`
- `decisionDrifted`
- `endAssignment`
- `factHistory`
- `findAssignmentState`
- `freezePosition`
- `inTeam`
- `mayContain`
- `mayRedelegate`
- `mergePositions`
- `planTermTransition`
- `positionMayBeFilled`
- `redelegate`
- `resolveAsOf`
- `seatIsOpen`
- `seatIsVacant`
- `splitPosition`
- `stateAuthorityAt`
- `succeedsTo`
- `teamConfers`
- `transferPosition`
- `typeOf`
- `unfreezePosition`
- `validateAssignmentCatalog`

## 4. The 45 canonical objects against what exists

Parsed from the source document's own §4 list, so an edit there reds this rather
than silently shrinking the denominator.

| Object | Status | Evidence | Note |
| --- | --- | --- | --- |
| `Person` | PARTIAL | `apps/web/src/lib/directory.ts` | Two person tables — `DirectoryPerson` and `User` — joined by email. No identity resolution and no privacy subject. |
| `Name` | PARTIAL | `apps/web/prisma/schema.prisma` | A single `name String` column. No parts, no locale, no preferred/legal distinction. |
| `ContactPoint` | PARTIAL | `apps/web/prisma/schema.prisma` | An `email` column, unique per table. No object, no type, no verification state. |
| `Worker` | ABSENT | — | No employment relationship to a legal employer exists anywhere in the schema. |
| `EmploymentRelationship` | ABSENT | — | No legal employer is modelled, so nothing can relate a person to one. |
| `Assignment` | PARTIAL | `packages/organization-model/src/assignment-states.ts` | Two incompatible tables (`RoleAssignment` dates, `SeatHolding` terms) and one unwired state catalogue. |
| `Job` | ABSENT | — | `Role` carries the title, but there is no reusable classification shared across organisations. |
| `JobFamily` | ABSENT | — | Nothing groups jobs. |
| `Position` | PARTIAL | `packages/organization-model/src/position-lifecycle.ts` | `Seat` is an effective-dated position with a `positionCode`. No headcount, no FTE, no funding, no hierarchy. |
| `Seat` | PRESENT | `apps/web/src/lib/seat-is-not-a-role.itest.ts` | A durable position separated from the authority it confers, with a test that fails when the two are re-merged. |
| `Grade` | ABSENT | — | No grade, ladder or rate exists. |
| `GradeRate` | ABSENT | — | No grade, ladder or rate exists. |
| `Location` | ABSENT | — | No location table. Events carry a free-text location string. |
| `WorkPattern` | ABSENT | — | No work pattern, shift or availability model. |
| `CollectiveAgreement` | ABSENT | — | No union or collective group is modelled. |
| `Candidate` | ABSENT | — | No recruiting relationship, and no purpose-limited data partition to hold one. |
| `Requisition` | ABSENT | — | Nothing authorises a vacancy. |
| `Application` | ABSENT | — | No application record. |
| `Interview` | ABSENT | — | No interview record. |
| `Offer` | ABSENT | — | No offer record. |
| `OnboardingJourney` | ABSENT | — | The handoff page assembles a packet at read time; nothing persists a journey, its tasks or its readiness. |
| `TransitionPlan` | PARTIAL | `apps/web/src/app/(app)/orgs/[slug]/handoff/page.tsx` | A handover packet rendered from live data, plus an unwired `planTermTransition` in the organisation model. `RoleTransfer` covers institution roles only. |
| `TimeEntry` | ABSENT | — | No time capture of any kind. |
| `TimeCard` | ABSENT | — | No time capture of any kind. |
| `Shift` | ABSENT | — | No scheduling model. |
| `AbsencePlan` | ABSENT | — | No absence or leave model. |
| `AbsenceBalance` | ABSENT | — | No absence or leave model. |
| `LeaveRequest` | ABSENT | — | No absence or leave model. `ApprovalRequest` is about club activity, not leave. |
| `CompensationElement` | ABSENT | — | No compensation model. The platform records club budgets, not pay. |
| `CompensationCycle` | ABSENT | — | No compensation model. |
| `BenefitPlan` | ABSENT | — | No benefits model. |
| `Enrollment` | ABSENT | — | No benefits model. |
| `Goal` | ABSENT | — | No goals, check-ins or reviews. |
| `Review` | ABSENT | — | No goals, check-ins or reviews. |
| `Feedback` | ABSENT | — | No feedback record. |
| `Skill` | ABSENT | — | No skills, competencies, licences or certifications. |
| `Profile` | ABSENT | — | No requirement profile separate from a person profile. |
| `LearningItem` | ABSENT | — | No learning catalogue. |
| `LearningAssignment` | ABSENT | — | No learning catalogue. |
| `SuccessionPlan` | PARTIAL | `packages/organization-model/src/succession-release.ts` | `planHandover` and `releaseToSuccessor` decide what a successor may inherit, and HCM-040-003 wired them to `apps/web/src/lib/people/seat-memory-boundary.ts` and the two memory read paths. Still no table: the plan is computed per request, never stored, so nothing records what a past handover actually released. |
| `HRCase` | ABSENT | — | No confidential case intake, classification or investigation record. |
| `PayrollRelationship` | ABSENT | — | No payroll object, and the architecture document forbids building one — see the contradiction recorded below. |
| `PayrollInput` | ABSENT | — | No payroll object. |
| `PayrollRunReference` | ABSENT | — | No payroll object. |
| `WorkforceEvent` | ABSENT | — | `AuditEvent` records who did what to a row; it is not a workforce event and carries no effective date. |

## 5. The 10 distinctions §2 requires be kept apart

| Distinction | Here | Why |
| --- | --- | --- |
| `Person` | SPLIT WRONGLY | `DirectoryPerson` and `User` are two person records for one human, joined by email. |
| `Worker` | ABSENT | No employment relationship exists to distinguish from membership. |
| `Member` | PRESENT | `InstitutionMembership` with status and an effective window; `packages/identity/src/effective-state.ts` is the one definition of live. |
| `Candidate` | ABSENT | No recruiting relationship exists. |
| `Dependent` | ABSENT | No related-person data, and no partition that could hold it. |
| `Job` | CONFLATED | `Role` is the job classification and the authority grant in one row. |
| `Position` | PRESENT | `Seat`, separated from `Role` so a rename cannot move authority. |
| `Seat` | PRESENT | `Seat` plus `MemoryRecord` as the continuity anchor. |
| `Assignment` | SPLIT WRONGLY | `RoleAssignment` (dates) and `SeatHolding` (academic-year strings) are two answers to one question. |
| `Delegation` | PRESENT | `ApprovalDelegation` is scoped, revocable and never becomes an assignment. |

`SPLIT WRONGLY` is the finding worth acting on first. One human is a
`DirectoryPerson` and a `User`, joined by email — and `email-is-not-a-key` is
already a standing security guard in this repository. One placement is a
`RoleAssignment` with dates and a `SeatHolding` with an academic-year string,
and no code reconciles them.

## 6. Claims audit

What the repository says about people and HR capability, each verified by opening
the file. `BACKED` — the code does what the words say. `HONEST_GAP` — the words
declare an absence and the absence is real. `CONTRADICTED` — two documents here
disagree and nobody has decided.

| Path | Claim | Verdict | Note |
| --- | --- | --- | --- |
| `apps/web/prisma/schema.prisma` | `Past terms are kept rather than overwritten` | BACKED | `SeatHolding` is unique on `(roleId, personId, term)` and prior terms survive. |
| `apps/web/prisma/schema.prisma` | `The durable organizational position, separate from the authority it carries.` | BACKED | `Seat` really is separate from `Role`, and an integration test fails if they are merged. |
| `blueprints/corporate-divisions/blueprint.ts` | `which this platform does not run` | HONEST_GAP | The corporate blueprint states that payroll is out of scope where a reader would otherwise assume it. |
| `docs/architecture/PLATFORM-ARCHITECTURE.md` | `Do not build payroll. Ever.` | CONTRADICTED | The People source document §3.10 defines five payroll modes including `TENURE_NATIVE_CERTIFIED` for an exact legal entity. The architecture document forbids the mode outright. Nobody has decided which governs. |
| `docs/architecture/REVIEW-FINDINGS.md` | `Three mutually exclusive target schemas, all marked MVP` | CONTRADICTED | P0-8 finds `Role`→`Seat`, a `position`/`person`/`role_assignment` rewrite, and "do not rename" all marked build-now. Every people model this domain would add lands on top of that unresolved choice. |
| `docs/architecture/REVIEW-FINDINGS.md` | `grants full authority to suspended and disabled people` | CONTRADICTED | P0-4 finds the effective-permission rule omits membership state, so a suspended member keeps every capability. The people model cannot be called correct while the rule that reads it is not. |
| `modules/index.ts` | `No domain event is published for a seat change` | HONEST_GAP | The roster module declares that a retried seat write is a second write. The source document requires idempotent people commands. |
| `modules/index.ts` | `No payment rail, card feed or payroll connector` | HONEST_GAP | The reimbursement module declares the absence of a payroll connector rather than implying one. |
| `modules/index.ts` | `lifecycle: "certified-limited"` | BACKED | The roster module ships `certified-limited`, not certified. It does not claim general availability. |
| `modules/index.ts` | `no duplicate-person or orphan-seat data-quality check` | HONEST_GAP | Declared. The source document's data-quality requirement is unmet and says so. |

No surface in `apps/web` claims payroll, benefits, time, absence, compensation,
recruiting or performance. The false-claim risk in this domain is not in the
product; it is in the three contradictions above, which a reader resolves by
picking whichever document they opened.

## 7. What this inventory does not tell you

- It reports that a module exists and still contains its anchor. It does not
  report that the module is correct, and `PARTIAL` never means working.
- Importers are found by module specifier. A relative import between siblings
  is invisible to it, so section 3's "reached by nothing" is a claim about
  cross-directory reach, not about every possible reference.
- Only models that own a relation into the workforce core are derived. A future
  people table that points at `User` alone has to be declared by hand, exactly
  as the three in section 1 are.
