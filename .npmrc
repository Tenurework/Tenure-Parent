# The Docker build installs with `npm ci --legacy-peer-deps` (react 19 peer
# ranges in the auth/aria dependencies are why). CI runs a bare `npm ci`.
# Pinning the flag here keeps npm install, npm ci, CI and Docker resolving the
# same tree instead of relying on a lockfile that happens to agree.
legacy-peer-deps=true
